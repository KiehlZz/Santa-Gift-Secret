# 🎄 Secret Santa 2026 - Gift Exchange System

ระบบจับฉลากแลกของขวัญปีใหม่สำหรับบริษัท/องค์กร

## ✨ Features

- 🎁 จับฉลากอัตโนมัติ (ไม่มีใครได้ของตัวเอง)
- 🔄 ป้องกัน 2-cycle (A ให้ B และ B ให้ A)
- 🔐 ระบบแอดมินสำหรับจัดการ
- 📱 รองรับการใช้งานบนมือถือ
- ❄️ UI สวยงามพร้อมเอฟเฟกต์หิมะตก

## 🚀 การติดตั้ง (Local Development)

### ข้อกำหนดเบื้องต้น
- Node.js >= 18.0.0
- npm หรือ yarn

### ขั้นตอนการติดตั้ง

1. Clone โปรเจค
```bash
git clone <your-repo-url>
cd secret-santa
```

2. ติดตั้ง Dependencies
```bash
cd server
npm install
```

3. สร้างไฟล์ `.env` (ถ้ายังไม่มี)
```bash
echo "ADMIN_PASSWORD=admin2026" > .env
echo "PORT=3000" >> .env
```

4. รันเซิร์ฟเวอร์
```bash
npm start
# หรือใช้ dev mode
npm run dev
```

5. เปิดเบราว์เซอร์
- พนักงาน: http://localhost:3000/participant
- แอดมิน: http://localhost:3000/admin

## 📦 Deploy บน Render

### ขั้นตอนที่ 1: เตรียมโปรเจค

1. **ตรวจสอบโครงสร้างไฟล์**
```
your-project/
├── client/
│   ├── admin.html
│   ├── admin.js
│   ├── participant.html
│   ├── participant.js
│   └── styles.css
├── server/
│   ├── server.js
│   ├── package.json
│   └── package-lock.json
├── .gitignore
└── README.md
```

2. **Push โค้ดขึ้น GitHub**
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <your-github-repo>
git push -u origin main
```

### ขั้นตอนที่ 2: สร้าง Web Service บน Render

1. ไปที่ https://render.com และ Login
2. คลิก "New +" → "Web Service"
3. เชื่อม GitHub repository ของคุณ
4. ตั้งค่าดังนี้:

**Build & Deploy Settings:**
- **Name:** `secret-santa-2026` (หรือชื่อที่คุณต้องการ)
- **Region:** Singapore (หรือใกล้ที่สุด)
- **Branch:** `main`
- **Root Directory:** `server`
- **Runtime:** `Node`
- **Build Command:** `npm install`
- **Start Command:** `node server.js`
- **Plan:** `Free`

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

ใน Render Dashboard → คลิก Web Service → ไปที่ **Environment**:

เพิ่ม Environment Variables:
- `ADMIN_PASSWORD` = `your-secure-password` (เปลี่ยนเป็นรหัสผ่านที่ปลอดภัย)
- `PORT` = `3000` (Render จะ override อัตโนมัติ)
- `NODE_ENV` = `production`

### ขั้นตอนที่ 4: Deploy

1. คลิก "Create Web Service"
2. รอให้ build และ deploy เสร็จ (ประมาณ 2-5 นาที)
3. เมื่อเสร็จจะได้ URL เช่น: `https://your-app-name.onrender.com`

### ขั้นตอนที่ 5: ทดสอบ

เปิด URL ที่ได้:
- หน้าพนักงาน: `https://your-app-name.onrender.com/participant`
- หน้าแอดมิน: `https://your-app-name.onrender.com/admin`

## ⚙️ Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `ADMIN_PASSWORD` | รหัสผ่านสำหรับแอดมิน | `admin2026` |
| `PORT` | Port ที่เซิร์ฟเวอร์จะรัน | `3000` |
| `NODE_ENV` | Environment mode | `development` |

## 🔧 Troubleshooting

### ปัญหา: Deploy ล้มเหลว
**แก้ไข:**
1. ตรวจสอบว่า Root Directory ตั้งเป็น `server`
2. ตรวจสอบ Build Command: `npm install`
3. ตรวจสอบ Start Command: `node server.js`

### ปัญหา: ไม่สามารถเชื่อมต่อ API
**แก้ไข:**
1. เปิด Browser Console (F12) ดู error
2. ตรวจสอบว่า API endpoints ถูกต้อง
3. ตรวจสอบ CORS settings

### ปัญหา: แอดมินเข้าไม่ได้
**แก้ไข:**
1. ตรวจสอบ `ADMIN_PASSWORD` ใน Environment Variables
2. ลอง reset รหัสผ่านใน Render Dashboard

### ปัญหา: Free Tier หยุดทำงาน
**หมายเหตุ:** Render Free Tier จะหยุดทำงานหลังไม่มีการเข้าถึง 15 นาที
- เมื่อมีการเข้าใช้งานใหม่จะ restart อัตโนมัติ (ใช้เวลาประมาณ 30-50 วินาที)
- หากต้องการให้ทำงานตลอดเวลา แนะนำอัพเกรดเป็น Paid Plan

## 📝 API Endpoints

### Public APIs
- `GET /status` - ดูสถานะระบบ
- `POST /register` - ลงทะเบียนผู้เข้าร่วม
- `GET /participants` - ดูรายชื่อทั้งหมด
- `GET /result/:name` - ดูผลการจับฉลาก

### Admin APIs
- `POST /admin/verify` - ตรวจสอบรหัสแอดมิน
- `GET /admin/results` - ดูผลลัพธ์ทั้งหมด (Admin only)
- `DELETE /participants/:name` - ลบผู้เข้าร่วม
- `POST /draw` - จับฉลาก
- `DELETE /reset` - รีเซ็ทระบบ

## 🔒 Security

- รหัสผ่านแอดมินเก็บใน Environment Variables
- Session management ด้วย sessionStorage
- Rate limiting: 100 requests/15 minutes
- CORS protection
- Input validation

## 📱 Browser Support

- Chrome (แนะนำ)
- Firefox
- Safari
- Edge
- Mobile browsers

## 📄 License

MIT License

## 👨‍💻 Author

Created for Secret Santa Gift Exchange 2026

## 🆘 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Troubleshooting section
2. ดู Render logs ใน Dashboard
3. ตรวจสอบ Browser Console (F12)