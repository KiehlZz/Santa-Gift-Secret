# 🎁 Secret Santa 2026 - Client-Server Version

ระบบจับฉลากแลกของขวัญปีใหม่แบบ Client-Server Architecture

## 📁 โครงสร้างโปรเจค

```
secret-santa-app/
├── server/                 # Backend (Node.js + Express)
│   ├── server.js          # ไฟล์ Server หลัก
│   ├── package.json       # การจัดการ dependencies
│   └── data.json          # Database (สร้างอัตโนมัติ)
│
└── client/                 # Frontend (HTML + CSS + JS)
    ├── index.html         # หน้าเว็บหลัก
    ├── styles.css         # การจัดการหน้าตา UI
    └── app.js             # JavaScript สำหรับเรียก API
```

## 🚀 วิธีการติดตั้งและรัน

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
# เข้าไปในโฟลเดอร์ server
cd server

# ติดตั้ง Node.js packages
npm install
```

### ขั้นตอนที่ 2: เริ่มต้น Server

```bash
# อยู่ในโฟลเดอร์ server
npm start

# หรือใช้ nodemon สำหรับ auto-reload (สำหรับการพัฒนา)
npm run dev
```

Server จะทำงานที่: `http://localhost:3000`

### ขั้นตอนที่ 3: เปิดเว็บไซต์

เปิดเบราว์เซอร์และเข้าไปที่: `http://localhost:3000`

## 📚 การทำงานของระบบ

### Backend (Server)

**ไฟล์: `server/server.js`**

Server ใช้ **Express.js** เป็น Web Framework และมี API ดังนี้:

#### 📡 API Endpoints

| Method | Endpoint | คำอธิบาย |
|--------|----------|----------|
| GET | `/api/status` | ดูสถานะระบบ (จำนวนคน, สถานะการจับฉลาก) |
| POST | `/api/register` | ลงทะเบียนผู้เข้าร่วม (ส่งชื่อไป) |
| GET | `/api/participants` | ดูรายชื่อผู้เข้าร่วมทั้งหมด |
| DELETE | `/api/participants/:name` | ลบผู้เข้าร่วมคนใดคนหนึ่ง |
| POST | `/api/draw` | จับฉลาก (ใช้ Derangement Algorithm) |
| GET | `/api/result/:name` | ดูผลการจับฉลากของแต่ละคน |
| DELETE | `/api/reset` | รีเซ็ตระบบทั้งหมด |

#### 🗄️ Data Storage

ข้อมูลถูกเก็บในไฟล์ `data.json` โดยมีโครงสร้าง:

```json
{
  "participants": ["Alice", "Bob", "Charlie"],
  "results": {
    "Alice": "Bob",
    "Bob": "Charlie",
    "Charlie": "Alice"
  },
  "isDrawn": true
}
```

#### 🧮 Derangement Algorithm

อัลกอริทึมสำหรับจับฉลากที่รับประกันว่า:
- ✅ ไม่มีใครได้ของตัวเอง
- ✅ ทุกคนได้ของพอดี 1 คน
- ✅ ไม่มีใครเหลือ

**วิธีการทำงาน:**
1. สุ่มเรียงลำดับใหม่ (Fisher-Yates Shuffle)
2. ตรวจสอบว่ามีใครอยู่ในตำแหน่งเดิมหรือไม่
3. ถ้ามี → สุ่มใหม่
4. ถ้าไม่มี → ใช้ผลลัพธ์นี้

### Frontend (Client)

**ไฟล์: `client/app.js`**

Frontend ใช้ **Vanilla JavaScript** เรียก API ผ่าน **Fetch API**

#### 🔄 การเรียก API

ตัวอย่างการเรียก API:

```javascript
// GET Request
const response = await fetch('http://localhost:3000/api/status');
const data = await response.json();

// POST Request
const response = await fetch('http://localhost:3000/api/register', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({ name: 'Alice' })
});
const data = await response.json();

// DELETE Request
const response = await fetch('http://localhost:3000/api/participants/Alice', {
    method: 'DELETE'
});
const data = await response.json();
```

#### 📱 ฟีเจอร์หลัก

1. **หน้าลงทะเบียน**
   - กรอกชื่อและส่งไปยัง Server
   - ตรวจสอบผลการจับฉลาก

2. **หน้าแอดมิน**
   - ดูรายชื่อผู้เข้าร่วมทั้งหมด
   - ลบผู้เข้าร่วม
   - กดปุ่มจับฉลาก
   - รีเซ็ตระบบ

## 🎯 ขั้นตอนการใช้งาน

### สำหรับพนักงาน (ผู้เข้าร่วม)
1. เปิดเว็บไซต์ที่ `http://localhost:3000`
2. กรอกชื่อในหน้า "ลงทะเบียน"
3. รอผู้จัดกดปุ่ม "Happy New Year 2026"
4. กดปุ่ม "ตรวจสอบผลของฉัน" เพื่อดูว่าได้ของใคร

### สำหรับผู้จัด (แอดมิน)
1. คลิกแท็บ "🔐 แอดมิน"
2. ดูรายชื่อผู้เข้าร่วมทั้งหมด
3. เมื่อครบทุกคน กดปุ่ม "🎊 Happy New Year 2026 🎊"
4. แจ้งให้ทุกคนกลับมาตรวจสอบผล

## 🔧 เทคโนโลยีที่ใช้

### Backend
- **Node.js**: JavaScript Runtime
- **Express.js**: Web Framework
- **CORS**: อนุญาตให้ Client เรียก API ข้าม domain
- **File System (fs)**: อ่าน/เขียนไฟล์ JSON

### Frontend
- **HTML5**: โครงสร้างหน้าเว็บ
- **CSS3**: การออกแบบ UI/UX
- **Vanilla JavaScript**: ตัวจัดการ Logic
- **Fetch API**: เรียก REST API
- **LocalStorage**: เก็บชื่อผู้ใช้ชั่วคราว

## 📖 สิ่งที่ได้เรียนรู้จากโปรเจคนี้

### 1. **HTTP Methods**
- **GET**: ดึงข้อมูล (Read)
- **POST**: สร้างข้อมูลใหม่ (Create)
- **PUT**: อัพเดทข้อมูล (Update)
- **DELETE**: ลบข้อมูล (Delete)

### 2. **RESTful API Design**
- การออกแบบ Endpoint ที่เข้าใจง่าย
- การใช้ HTTP Status Code ที่ถูกต้อง
  - 200: สำเร็จ
  - 201: สร้างสำเร็จ
  - 400: ข้อมูลผิดพลาด
  - 404: ไม่พบข้อมูล
  - 500: เกิดข้อผิดพลาดใน Server

### 3. **Client-Server Communication**
- การส่งข้อมูลผ่าน JSON
- การจัดการ Async/Await
- การจัดการ Error

### 4. **File Management**
- การอ่าน/เขียนไฟล์ด้วย Node.js
- การจัดการ JSON Database

### 5. **Frontend-Backend Integration**
- การเชื่อมต่อ Frontend กับ Backend
- การแสดงผลข้อมูลแบบ Real-time
- การจัดการ State

## 🐛 การแก้ปัญหาทั่วไป

### ปัญหา: Server ไม่ทำงาน
```bash
# ตรวจสอบว่า Node.js ติดตั้งแล้ว
node --version

# ติดตั้ง dependencies ใหม่
cd server
npm install
```

### ปัญหา: ไม่สามารถเชื่อมต่อกับ Server
- ตรวจสอบว่า Server ทำงานอยู่ที่ port 3000
- ตรวจสอบ URL ใน `client/app.js` (ต้องเป็น `http://localhost:3000`)

### ปัญหา: CORS Error
- Server มี CORS middleware ติดตั้งอยู่แล้ว
- ถ้ายังมีปัญหา ให้เปิดเว็บผ่าน `http://localhost:3000` (ไม่ใช่ file://)

## 🎓 แนวทางการพัฒนาต่อ

1. **เพิ่ม Database จริง**: ใช้ MongoDB, PostgreSQL หรือ MySQL แทน JSON file
2. **เพิ่ม Authentication**: ใช้ JWT สำหรับ login ระบบแอดมิน
3. **เพิ่ม WebSocket**: สำหรับการอัพเดทข้อมูล Real-time
4. **Deploy ขึ้น Cloud**: Heroku, Vercel, AWS, Google Cloud
5. **เพิ่ม Email Notification**: ส่งอีเมลแจ้งผลการจับฉลาก
6. **เพิ่ม Unit Tests**: Jest, Mocha สำหรับ testing

## 📝 License

MIT License - ใช้งานได้อย่างอิสระ

---

**สร้างด้วย ❤️ สำหรับการเรียนรู้ Web Development**